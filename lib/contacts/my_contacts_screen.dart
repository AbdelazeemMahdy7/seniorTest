import 'package:flutter/material.dart';

class MyContactsScreen extends StatelessWidget {
  const MyContactsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Contacts"),);
  }
}
